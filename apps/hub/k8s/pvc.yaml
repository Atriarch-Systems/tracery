apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tracery-hub-data
  namespace: tracery
  labels:
    app.kubernetes.io/name: tracery-hub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName: truenas-ssd   # set to the cluster's default/preferred class
