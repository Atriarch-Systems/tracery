apiVersion: v1
kind: Service
metadata:
  name: activity-hub
  namespace: activity
  labels:
    app.kubernetes.io/name: activity-hub
spec:
  selector:
    app.kubernetes.io/name: activity-hub
  ports:
    - name: http
      port: 8971
      targetPort: http
