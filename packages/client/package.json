{
  "name": "@atriarch/activity-client",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Atriarch Activity emitter SDK: ActivityTracer, transports and a hub read client.",
  "type": "module",
  "files": ["dist", "README.md", "LICENSE"],
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@atriarch/activity-core": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
