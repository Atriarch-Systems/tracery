{
  "name": "@atriarch/activity-react",
  "version": "0.1.0",
  "license": "MIT",
  "description": "ActivityExplorer: a composite React explorer (flow picker, guided graph, inspector) and live-source hooks for Atriarch Activity.",
  "type": "module",
  "files": ["dist", "README.md", "LICENSE"],
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "prepack": "npm run build"
  },
  "peerDependencies": {
    "react": ">=18 <20",
    "react-dom": ">=18 <20"
  },
  "dependencies": {
    "@atriarch/activity-core": "*",
    "@atriarch/activity-visualizer": "*",
    "@atriarch/activity-client": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/react": "^19.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
